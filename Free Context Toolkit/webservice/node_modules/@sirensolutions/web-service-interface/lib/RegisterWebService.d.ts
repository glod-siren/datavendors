import { ServiceDefinition } from './ServiceDefinition';
import { Schema } from 'joi';
/**
 * Registers a list of services under a service group name.
 */
export declare function registerServices(serviceGroup: string, Services: ServiceDefinitionClass[], configSchema?: ConfigSchema): any;
declare type ServiceDefinitionClass = new (config: any) => ServiceDefinition;
interface ConfigSchema {
    [param: string]: Schema;
}
export {};
